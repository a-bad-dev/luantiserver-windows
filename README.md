# luantiserver-windows

Builds of luantiserver for Windows x86_64.

They are built and tested on Windows 10 (in a VM) but should work on Windows 8, 8.1, 10, and 11.

The `bundle_dlls.sh` script is from [here](https://github.com/rollerozxa/msys2-bundledlls).
